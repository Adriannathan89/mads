# Copy this file to the ignored `.env` for local development.
# Use a real secret through the process environment in CI and production.
DATABASE_URL=postgres://mads:mads@127.0.0.1:5432/mads

# Development-only placeholder; replace with at least 32 random bytes.
JWT_SECRET=replace-with-at-least-32-random-bytes
